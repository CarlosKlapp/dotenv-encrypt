/**
 * The encryption key name cannot be one of the reserved words.
 */
export const keyNames: string[] = ["WILDCARD"];

/**
 * The encryption key name cannot be one of the reserved words.
 */
export const keyNamesForCipheredVariable: string[] = [
    "VERY_LONG_IMPROBABLE_RESERVED_WORD_USED_FOR_TESTING",
];

/**
 * The encryption key name cannot be one of the reserved words.
 */
export const keyNamesForPlainTextVariable: string[] = ["WILDCARD"];
